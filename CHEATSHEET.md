# PsychoTeleBot - Шпаргалка по командам

## 🚀 Быстрый запуск

```bash
# Установка
pip install -r requirements.txt

# Тесты
pytest -v

# Демо
python demo.py

# CLI (интерактивный режим)
python -m adapters.cli
# или
run_cli.bat  # Windows
./run_cli.sh # Linux/Mac
```

## 📝 Команды в CLI

| Команда | Описание |
|---------|----------|
| `/start` | Начать диалог |
| `/menu` | Вернуться в меню |
| `/clear` | Очистить контекст ИИ |
| `/reset` | Сбросить сессию |
| `/user <id>` | Сменить пользователя |
| `/tickets` | Показать заявки |
| `/quit` | Выход |

## 🎮 Меню бота

| Номер | Действие |
|-------|----------|
| 1 | Консультация со специалистом |
| 2 | Консультация с ИИ |
| 3 | Условия обращения |
| 4 | Вопрос по психологии |

## 🧪 Команды pytest

```bash
# Все тесты
pytest

# С подробностями
pytest -v

# Конкретный файл
pytest tests/test_bot_service.py

# Конкретный тест
pytest tests/test_bot_service.py::test_consultation_full_flow

# С покрытием
pip install pytest-cov
pytest --cov=. --cov-report=html
```

## 📂 Структура проекта

```
PsychoTeleBot/
├── domain/              # Модели
├── application/         # Логика
├── infrastructure/      # Реализации
├── adapters/            # CLI
│   └── cli/
└── tests/              # Тесты
```

## 🔧 Полезные файлы

- `README.md` - основная документация
- `ARCHITECTURE.md` - архитектура
- `QUICKSTART.md` - быстрый старт
- `COMPLETION_REPORT.md` - отчет о выполнении

## 💡 Примеры использования

### Создать заявку
```
> /start
> 1
> Депрессия
> Мужской
> 30
> 3
> Опишите проблему
```

### Чат с ИИ
```
> /start
> 2
> Ваш вопрос
> /clear  # очистить контекст
> /menu   # в меню
```

## 🎯 Быстрая диагностика

```bash
# Проверка работы
python demo.py

# Проверка тестов
pytest --tb=short

# Проверка структуры
tree /F  # Windows
ls -R    # Linux/Mac
```

## 📞 Состояния бота

1. MENU - главное меню
2. CONSULT_FORM_* - форма консультации
3. AI_CHAT - чат с ИИ
4. TERMS - условия
5. PSY_QUESTION - вопрос

## ⚙️ Настройка

### Изменить хранилище
```python
# В adapters/cli/runner.py
# Было:
session_repo = InMemorySessionRepository()

# Стало:
session_repo = SQLiteSessionRepository("bot.db")
```

### Добавить логирование
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

---

**Еще вопросы?** Смотрите `QUICKSTART.md` или `README.md`
