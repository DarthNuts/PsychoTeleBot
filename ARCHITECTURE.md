# Архитектура проекта PsychoTeleBot

## Обзор

Проект построен на принципах **Clean Architecture**, что обеспечивает:
- 🎯 Независимость бизнес-логики от внешних фреймворков
- 🧪 Полную тестируемость без внешних зависимостей
- 🔄 Легкую замену компонентов (репозитории, адаптеры)
- 📦 Четкое разделение ответственности

## Структура проекта

```
PsychoTeleBot/
│
├── domain/                      # 🏛️ Доменный слой (ядро бизнес-логики)
│   ├── __init__.py
│   ├── models.py               # Модели данных (State, Ticket, UserSession, etc.)
│   └── repositories.py         # Интерфейсы репозиториев (абстракции)
│
├── application/                 # 🎮 Слой приложения (use cases)
│   ├── __init__.py
│   ├── state_machine.py        # State Machine для управления состояниями
│   └── bot_service.py          # Основной сервис бота (координатор)
│
├── infrastructure/              # 🔧 Слой инфраструктуры (реализации)
│   ├── __init__.py
│   └── in_memory_repositories.py  # In-memory реализации репозиториев
│
├── adapters/                    # 🔌 Адаптеры (интерфейсы к внешнему миру)
│   ├── __init__.py
│   └── cli/                    # CLI адаптер для отладки
│       ├── __init__.py
│       ├── __main__.py
│       └── runner.py
│
├── tests/                       # 🧪 Тесты
│   ├── __init__.py
│   ├── test_models.py          # Тесты моделей
│   ├── test_state_machine.py  # Тесты state machine
│   └── test_bot_service.py    # Интеграционные тесты
│
├── requirements.txt             # Зависимости Python
├── README.md                    # Основная документация
├── demo.py                      # Демонстрационный скрипт
├── run_cli.bat                  # Запуск CLI (Windows)
├── run_cli.sh                   # Запуск CLI (Linux/Mac)
└── .gitignore                   # Git ignore файл
```

## Слои архитектуры

### 1. Domain Layer (Доменный слой) 🏛️

**Назначение:** Ядро бизнес-логики, полностью независимое от внешних фреймворков.

**Файлы:**
- `models.py` — доменные модели и enums
- `repositories.py` — интерфейсы (абстракции) для работы с данными

**Модели:**
- `State` — состояния пользователя в боте
- `TicketStatus` — статусы заявок
- `Severity` — уровни критичности
- `ConsultationForm` — форма для консультации
- `Ticket` — заявка на консультацию
- `UserSession` — сессия пользователя

**Принципы:**
- ✅ Не зависит от других слоев
- ✅ Содержит только бизнес-правила
- ✅ Использует только стандартные типы Python

### 2. Application Layer (Слой приложения) 🎮

**Назначение:** Оркестрация бизнес-логики, реализация use cases.

**Файлы:**
- `state_machine.py` — конечный автомат для управления диалогом
- `bot_service.py` — главный сервис, координирующий все операции

**Компоненты:**

#### StateMachine
Управляет переходами между состояниями:
- Обрабатывает сообщения пользователя
- Определяет следующее состояние
- Валидирует ввод
- Формирует ответы

#### BotService
Координирует все операции бота:
- Использует StateMachine для обработки сообщений
- Управляет сессиями пользователей
- Создает и управляет заявками
- Связывает domain с infrastructure

**Принципы:**
- ✅ Зависит только от domain
- ✅ Использует абстракции (интерфейсы) из domain
- ✅ Реализует бизнес-процессы

### 3. Infrastructure Layer (Слой инфраструктуры) 🔧

**Назначение:** Конкретные реализации абстракций из domain.

**Файлы:**
- `in_memory_repositories.py` — реализация репозиториев в памяти

**Компоненты:**
- `InMemorySessionRepository` — хранение сессий в памяти
- `InMemoryTicketRepository` — хранение заявок в памяти

**Принципы:**
- ✅ Реализует интерфейсы из domain
- ✅ Легко заменяется (например, на SQLite, PostgreSQL)
- ✅ Изолирует детали хранения данных

**Возможные расширения:**
```python
# Пример: SQLite репозиторий
class SQLiteSessionRepository(SessionRepository):
    def get(self, user_id: str) -> Optional[UserSession]:
        # SQL запрос
        pass
```

### 4. Adapters Layer (Слой адаптеров) 🔌

**Назначение:** Интерфейсы к внешнему миру (CLI, Telegram API, REST API, etc.).

**Файлы:**
- `cli/runner.py` — CLI интерфейс для отладки
- `cli/__main__.py` — точка входа для `python -m adapters.cli`

**Компоненты:**

#### CLIRunner
Интерактивный CLI для отладки:
- Эмулирует взаимодействие с ботом
- Отображает состояние
- Поддерживает несколько пользователей
- Показывает заявки

**Принципы:**
- ✅ Использует только application layer
- ✅ Не содержит бизнес-логики
- ✅ Легко добавить другие адаптеры (Telegram, Discord, etc.)

**Расширение (пример Telegram адаптера):**
```python
# adapters/telegram/bot.py
from application.bot_service import BotService

class TelegramAdapter:
    def __init__(self, bot_service: BotService):
        self.bot_service = bot_service
    
    async def handle_message(self, update):
        user_id = str(update.message.from_user.id)
        message = update.message.text
        response = self.bot_service.process_message(user_id, message)
        await update.message.reply_text(response)
```

## Поток данных

```
┌─────────────┐
│   User      │ (CLI, Telegram, etc.)
└─────┬───────┘
      │
      ▼
┌─────────────┐
│  Adapter    │ (cli/runner.py, telegram/bot.py)
└─────┬───────┘
      │
      ▼
┌─────────────┐
│ BotService  │ (application/bot_service.py)
└─────┬───────┘
      │
      ├──────────────┬──────────────┐
      ▼              ▼              ▼
┌──────────┐  ┌────────────┐  ┌─────────────┐
│   State  │  │ Repository │  │ Repository  │
│ Machine  │  │ (Session)  │  │ (Ticket)    │
└──────────┘  └────────────┘  └─────────────┘
      │              │              │
      ▼              ▼              ▼
┌─────────────────────────────────────────┐
│         Domain Models & Logic           │
│  (State, Ticket, UserSession, etc.)     │
└─────────────────────────────────────────┘
```

## State Machine

### Состояния:
1. `MENU` — главное меню
2. `CONSULT_FORM_TOPIC` — ввод темы
3. `CONSULT_FORM_GENDER` — ввод пола
4. `CONSULT_FORM_AGE` — ввод возраста
5. `CONSULT_FORM_SEVERITY` — выбор критичности
6. `CONSULT_FORM_MESSAGE` — ввод описания
7. `AI_CHAT` — чат с ИИ
8. `TERMS` — условия обращения
9. `PSY_QUESTION` — вопрос по психологии

### Переходы:

```
             /start, /menu
                  │
                  ▼
         ┌────────────────┐
         │     MENU       │◄───────────┐
         └────────────────┘            │
                  │                    │
      ┌───────────┼──────────┬────────┤
      │           │          │        │
      ▼           ▼          ▼        │
┌──────────┐ ┌─────────┐ ┌──────┐   │
│Consult   │ │AI_CHAT  │ │TERMS │   │
│Form Flow │ │         │ │      │   │
└──────────┘ └─────────┘ └──────┘   │
      │           │          │       │
      └───────────┴──────────┴───────┘
              /menu (global)
```

## Dependency Injection

Пример создания экземпляра бота:

```python
# 1. Создаем репозитории (infrastructure)
session_repo = InMemorySessionRepository()
ticket_repo = InMemoryTicketRepository()

# 2. Создаем state machine (application)
state_machine = StateMachine()

# 3. Создаем сервис бота (application)
bot_service = BotService(
    session_repo=session_repo,
    ticket_repo=ticket_repo,
    state_machine=state_machine
)

# 4. Используем в адаптере
response = bot_service.process_message(user_id, message)
```

## Тестирование

### Уровни тестов:

1. **Unit тесты моделей** (`test_models.py`)
   - Тестируют чистые модели данных
   - Не требуют зависимостей

2. **Unit тесты State Machine** (`test_state_machine.py`)
   - Тестируют логику переходов
   - Изолированы от репозиториев

3. **Интеграционные тесты** (`test_bot_service.py`)
   - Тестируют полные сценарии
   - Используют in-memory репозитории
   - Проверяют взаимодействие компонентов

### Принципы тестирования:
- ✅ Все тесты работают **офлайн**
- ✅ Нет зависимости от Telegram API
- ✅ Быстрое выполнение
- ✅ Детерминированные результаты

## Расширение функциональности

### Добавление новой базы данных:

1. Создайте файл `infrastructure/sqlite_repositories.py`
2. Реализуйте `SessionRepository` и `TicketRepository`
3. Замените при инициализации:

```python
# Было:
session_repo = InMemorySessionRepository()

# Стало:
session_repo = SQLiteSessionRepository("bot.db")
```

### Добавление Telegram адаптера:

1. Создайте `adapters/telegram/bot.py`
2. Используйте `BotService` для обработки сообщений
3. Telegram-специфичная логика только в адаптере

### Добавление нового состояния:

1. Добавьте состояние в `domain/models.py`:
```python
class State(str, Enum):
    # ...
    NEW_STATE = "NEW_STATE"
```

2. Добавьте обработчик в `application/state_machine.py`:
```python
def _handle_new_state(self, session, message):
    # Логика обработки
    return session, response
```

3. Зарегистрируйте в `handlers`:
```python
self.handlers = {
    # ...
    State.NEW_STATE: self._handle_new_state,
}
```

## Преимущества архитектуры

✅ **Тестируемость** — каждый слой тестируется независимо
✅ **Гибкость** — легко заменить любой компонент
✅ **Масштабируемость** — четкое разделение ответственности
✅ **Читаемость** — понятная структура
✅ **Поддерживаемость** — изолированные изменения

## Зависимости между слоями

```
Adapters ────> Application ────> Domain
                    │
                    ▼
            Infrastructure ────> Domain
```

**Правило:** Зависимости направлены только внутрь (к domain).

## Заключение

Эта архитектура позволяет:
- Разрабатывать и тестировать бота **без Telegram**
- Легко добавлять новые адаптеры (Discord, Slack, etc.)
- Менять способ хранения данных без изменения логики
- Поддерживать код в чистоте и порядке
